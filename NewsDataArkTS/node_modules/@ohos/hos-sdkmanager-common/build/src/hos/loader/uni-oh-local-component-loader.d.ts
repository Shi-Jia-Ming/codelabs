import { AbstractComponentDto, OhLocalComponentLoader, Progress } from '@ohos/sdkmanager-common';
export declare class UniOhLocalComponentLoader extends OhLocalComponentLoader {
    constructor(sdkRoot: string, progress?: Progress);
    protected getLocation(component: AbstractComponentDto, sdkRoot: string): string | undefined;
}
