/// <reference types="node" />
import { PathLike, Stats } from 'fs';
/**
 * 校验路径是否有写权限
 * @param path
 */
export declare function isWritable(path: PathLike): boolean;
/**
 * 当文件不存在时,fs.statSync方法会抛异常,需要catch一下
 * @param filePath
 * @private
 */
export declare function fsStateSync(filePath: string): Stats | undefined;
export declare function renameToForeignTarget(originFilePath: string, copyFilePath: string): void;
export declare function moveToForeignTarget(originFilePath: string, copyFilePath: string): void;
/**
 * 创建当前路径的上一级目录
 * @param location
 */
export declare function ensureParentDir(location: string): void;
