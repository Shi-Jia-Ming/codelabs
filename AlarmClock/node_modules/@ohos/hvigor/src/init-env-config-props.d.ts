/**
 * 加载liftOff获取到的configFiles中的具体一个配置文件的路径
 *
 * @param {any} env liftoff环境变量
 * @param {any} opts yargs解析的命令行参数
 * @return {string} path 返回cwd的路径
 */
export declare function initEnvConfigProps(env: any, opts: any): string;
