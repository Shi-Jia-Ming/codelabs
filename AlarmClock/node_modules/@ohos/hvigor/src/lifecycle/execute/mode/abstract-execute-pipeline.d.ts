import { HvigorNode, Module, Project } from '@ohos/hvigor-base';
import { TaskExecuteOptions } from './execute-mode-factory.js';
/**
 * 根据不用场景创建的pileLine的抽象公共类
 *
 * @since 2022/6/20
 */
export declare abstract class AbstractExecutePipeline {
    protected project: Project;
    protected taskOptions: TaskExecuteOptions;
    protected shouldCheckTask: boolean;
    protected constructor(project: Project, taskOptions: TaskExecuteOptions);
    /**
     * 开始执行该pipeline
     */
    startPipeline(): void;
    /**
     * 执行一个Node的sync同步任务
     *
     * @param {HvigorNode} hvigorNode
     * @protected
     */
    protected doOneNodeSync(hvigorNode: HvigorNode): void;
    /**
     * 执行一个Node的命令行中指定的任务
     *
     * @param {Project | Module} hvigorNode
     * @protected
     */
    protected doOneNodeTask(hvigorNode: Project | Module): void;
    /**
     * 初始化一个Node的打点数据
     */
    recordTraceData(): void;
    /**
     * 执行一个Mode的Sync
     */
    abstract executeSyncTask(): void;
    /**
     * 执行一个Mode的Task
     */
    abstract executeTask(): void;
}
