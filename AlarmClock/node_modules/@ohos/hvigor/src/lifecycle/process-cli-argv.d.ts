import { CliOptions } from '../options/cli-options.js';
/**
 * 打印一些基础信息,不需要在configuration hvigorfile.js之前的一些命令
 * 比如:--help --version
 *
 * @param {CliOptions} opts
 * @param {string} moduleVersion
 */
export declare function processArgvBeforeConfiguration(opts: CliOptions, moduleVersion: string): void;
