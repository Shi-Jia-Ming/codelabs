import { Component, ComponentDto } from '@ohos/sdkmanager-common';
import { AbstractLocalComponentLoader } from '@ohos/sdkmanager-common';
export default class HosLocalComponentLoader extends AbstractLocalComponentLoader {
    private static MAX_SCAN_DEPTH;
    private static UNI_PACKAGE_NAME;
    private static TOOLCHAINS_SET;
    constructor(sdkRoot: string);
    protected getUniPackageName(): string;
    protected configMeta(jsonObject: any, component: ComponentDto): void;
    protected addToMap(sdkMap: Map<string, Component>, component: Component): void;
    protected convertComponent(jsonObject: any): ComponentDto;
    protected isComponentInRightPlace(component: ComponentDto, uniPackage: string): boolean;
}
